{
  "version": "1.2",
  "created": "2026-06-16",
  "updated": "2026-06-18",
  "notification": {
    "targetKey": "何杨文",
    "channel": "weixin_fallback_feishu"
  },
  "databases": {
    "aipin_data": "/home/jedi/.local/share/jedi-web/aipin-data.sqlite",
    "wechat_765": "/www/jedi_web/wechat_765.sqlite"
  },
  "report_paths": {
    "daily": "/home/jedi/jedi-web-agent-output/reports/daily/",
    "weekly": "/home/jedi/jedi-web-agent-output/reports/weekly/",
    "monthly": "/home/jedi/jedi-web-agent-output/reports/monthly/"
  },
  "jobs": [
    {
      "id": "4a961a7f",
      "name": "服务器资源监控",
      "cron": "3,13,23,33,43,53 * * * *",
      "recurring": true,
      "description": "每10分钟推送服务器内存使用率和agent进程数",
      "template": "📊 服务器实时监控\n\n【内存使用率】\n| 指标 | 值 |\n|------|-----|\n| 总内存 | {total} |\n| 已用 | {used}（{percent}） |\n| 可用 | {available} |\n\n【Agent 处理情况】{count}个运行中\n| PID | 启动 | CPU | 内存(MB) |\n|-----|------|-----|-----|\n{agent_rows}",
      "prompt": "服务器资源监控定时推送任务。\n\n请执行以下步骤：\n1. 运行 `free -h` 获取内存使用情况\n2. 运行 `ps aux | grep -E \"agent|Agent\" | grep -v grep | wc -l` 统计当前 agent 进程数；同时运行 `ps aux | grep -E \"agent|Agent\" | grep -v grep | awk '{printf \"| %s | %s | %s%% | %.0fMB |\\n\", $2, $9, $3, $6/1024}'` 获取 agent 进程详情（PID、启动时间、CPU%、内存MB）\n3. 将结果格式化为如下排版：\n\n📊 服务器实时监控\n\n【内存使用率】\n| 指标 | 值 |\n|------|-----|\n| 总内存 | {total} |\n| 已用 | {used}（{percent}） |\n| 可用 | {available} |\n\n【Agent 处理情况】{count}个运行中\n| PID | 启动 | CPU | 内存(MB) |\n|-----|------|-----|-----|\n（agent进程列表）\n\n4. 使用 notification_send 工具发送通知，targetKey 为 \"何杨文\"，text 为上述格式化内容。如果微信通道不可用会自动回退到飞书。"
    },
    {
      "id": "1c975f7b",
      "name": "美的舆情统计",
      "cron": "7 * * * *",
      "recurring": true,
      "db": "/home/jedi/.local/share/jedi-web/aipin-data.sqlite",
      "description": "每小时推送美的舆情文章处理统计",
      "template": "📡 美的舆情监控\n\n【近1小时概况】{window}\n| 指标 | 数量 |\n|------|------|\n| 新接收 | {new_1h} 条 |\n| 处理完成 | {processed} 条 |\n| 推送成功 | {push_ok} 条 |\n| 推送失败 | {push_fail} 条 |\n\n【今日累计】{today_total} 条\n| 分类 | 数量 |\n|------|------|\n| 推送 | {t_push} |\n| 不推送 | {t_nopush} |\n| 重复 | {t_dup} |\n\n【全量数据】\n| 指标 | 数值 |\n|------|------|\n| 总计 | {total_all} |\n| 已推送 | {total_pushed} |\n| 失败 | {total_failed} |\n| 处理中 | {processing} |",
      "prompt": "📡 美的舆情监控 — 每小时统计推送。\n\n先执行 Python 查询：\n\n```python3\nimport sqlite3\nfrom datetime import datetime, timezone, timedelta\n\nconn = sqlite3.connect('/home/jedi/.local/share/jedi-web/aipin-data.sqlite')\nc = conn.cursor()\n\nnow = datetime.now(timezone.utc) + timedelta(hours=8)\nhour_start = now.replace(minute=0, second=0, microsecond=0) - timedelta(hours=1)\nhour_end = now.replace(minute=0, second=0, microsecond=0)\ntoday_start = now.replace(hour=0, minute=0, second=0, microsecond=0)\nutc_start = (hour_start - timedelta(hours=8)).strftime('%Y-%m-%dT%H:%M:%S')\nutc_end = (hour_end - timedelta(hours=8)).strftime('%Y-%m-%dT%H:%M:%S')\ntoday_utc = (today_start - timedelta(hours=8)).strftime('%Y-%m-%dT%H:%M:%S')\nwindow = f'{hour_start.strftime(\"%H:%M\")}-{hour_end.strftime(\"%H:%M\")}'\n\nc.execute(f\"SELECT COUNT(*) FROM aipin_data_items WHERE received_at >= '{utc_start}' AND received_at < '{utc_end}'\")\nnew_1h = c.fetchone()[0]\nc.execute(f\"SELECT COUNT(*) FROM aipin_data_items WHERE finished_at >= '{utc_start}' AND finished_at < '{utc_end}'\")\nprocessed_1h = c.fetchone()[0]\nc.execute(f\"SELECT COUNT(*) FROM aipin_data_items WHERE push_status='success' AND pushed_at >= '{utc_start}' AND pushed_at < '{utc_end}'\")\npush_ok = c.fetchone()[0]\nc.execute(f\"SELECT COUNT(*) FROM aipin_data_items WHERE push_status='failed' AND pushed_at >= '{utc_start}' AND pushed_at < '{utc_end}'\")\npush_fail = c.fetchone()[0]\n\nc.execute(f\"SELECT COUNT(*) FROM aipin_data_items WHERE received_at >= '{today_utc}'\")\ntoday_total = c.fetchone()[0]\nc.execute(f\"SELECT push_flag, COUNT(*) FROM aipin_data_items WHERE received_at >= '{today_utc}' GROUP BY push_flag\")\nflags = dict(c.fetchall())\n\nc.execute('SELECT COUNT(*) FROM aipin_data_items')\ntotal_all = c.fetchone()[0]\nc.execute(\"SELECT COUNT(*) FROM aipin_data_items WHERE push_status='success'\")\ntotal_pushed = c.fetchone()[0]\nc.execute(\"SELECT COUNT(*) FROM aipin_data_items WHERE process_status='failed'\")\ntotal_failed = c.fetchone()[0]\nc.execute(\"SELECT COUNT(*) FROM aipin_data_items WHERE process_status='received'\")\npending = c.fetchone()[0]\nc.execute(\"SELECT COUNT(*) FROM aipin_data_items WHERE process_status='processing'\")\nprocessing = c.fetchone()[0]\n\nprint(f'{window}|{new_1h}|{processed_1h}|{push_ok}|{push_fail}|{today_total}|{flags.get(\"推送\",0)}|{flags.get(\"不推送\",0)+(flags.get(None,0))}|{flags.get(\"重复\",0)}|{total_all}|{total_pushed}|{total_failed}|{pending}|{processing}')\n```\n\n然后按以下格式用 notification_send 发送，targetKey=\"何杨文\"：\n\n📡 美的舆情监控\n\n【近1小时概况】{WINDOW}\n| 指标 | 数量 |\n|------|------|\n| 新接收 | {new_1h} 条 |\n| 处理完成 | {processed} 条 |\n| 推送成功 | {push_ok} 条 |\n| 推送失败 | {push_fail} 条 |\n\n【今日累计】{today_total} 条\n| 分类 | 数量 |\n|------|------|\n| 推送 | {t_push} |\n| 不推送 | {t_nopush} |\n| 重复 | {t_dup} |\n\n【全量数据】\n| 指标 | 数值 |\n|------|------|\n| 总计 | {total_all} |\n| 已推送 | {total_pushed} |\n| 失败 | {total_failed} |\n| 处理中 | {processing} |"
    },
    {
      "id": "bb0773c9",
      "name": "科创巡检 · 采集",
      "cron": "30 3 * * *",
      "recurring": true,
      "db": "/www/jedi_web/wechat_765.sqlite",
      "description": "每天03:30检查02:00批次微信文章采集情况",
      "template": "📡 科创巡检 · 采集\n━━━━━━━━━━━━━━━\n📅 {today}\n\n{icon} 微信文章采集：{status}\n  · 02:00批次：{collected} 篇\n  · 成功：{success} | 缺失：{missing} | 空：{empty}\n  · 数据库更新：{db_mtime}\n\n{fix}",
      "prompt": "🔍 科创巡检 — 采集巡检 (03:30)\n\n请严格按照下面步骤执行，发现异常时必须深入分析原因并给出处理方案。\n\n## 1. 采集量检查\n\n```python3\nimport sqlite3, os\nfrom datetime import datetime, timezone, timedelta\n\nconn = sqlite3.connect('/www/jedi_web/wechat_765.sqlite')\nc = conn.cursor()\n\nnow = datetime.now(timezone.utc) + timedelta(hours=8)\ntoday_str = now.strftime('%Y-%m-%d')\nyesterday_utc = (now - timedelta(days=1)).strftime('%Y-%m-%d')\nutc_start = f'{yesterday_utc}T18:00:00'\nutc_end = f'{yesterday_utc}T18:45:00'\n\nc.execute(f\"SELECT COUNT(*) FROM wechat_articles WHERE collected_at >= '{utc_start}' AND collected_at < '{utc_end}'\")\ncollected = c.fetchone()[0]\nc.execute(f\"SELECT content_status, COUNT(*) FROM wechat_articles WHERE collected_at >= '{utc_start}' AND collected_at < '{utc_end}' GROUP BY content_status\")\nstatus = dict(c.fetchall())\ndb_mtime = datetime.fromtimestamp(os.path.getmtime('/www/jedi_web/wechat_765.sqlite')).strftime('%Y-%m-%d %H:%M')\n\n# Also get the latest collected_at to check when the last article was inserted\nc.execute('SELECT MAX(collected_at) FROM wechat_articles')\nlast_collected = c.fetchone()[0] or 'N/A'\n\nsuccess = status.get('success', 0)\nmissing = status.get('missing', 0)\nempty = status.get('empty', 0)\ntotal_status = collected\n\nif collected == 0:\n    icon, coll_status = '🔴', '异常'\nelif missing > collected * 0.3:\n    icon, coll_status = '⚠️', '警告'\nelse:\n    icon, coll_status = '✅', '正常'\n\nprint(f'DATE={today_str}|COLLECTED={collected}|SUCCESS={success}|MISSING={missing}|EMPTY={empty}|DB_MTIME={db_mtime}|ICON={icon}|STATUS={coll_status}|LAST_COLLECTED={last_collected}')\n```\n\n## 2. 如果 COLLECTED=0 或 STATUS=警告 或 STATUS=异常，必须执行以下排查：\n\n### 2.1 检查 cron 执行日志\n```bash\ntail -80 /www/jedi_web/logs/wechat-collector-cron.log 2>/dev/null | grep -E \"error|Error|ERROR|fail|Fail|FAIL|started|finished|command not found|npm run|backfill\" | tail -20\n```\n\n### 2.2 检查数据库备份\n```bash\nls -lt /www/jedi_web/backups/wechat_765.backup-*.sqlite 2>/dev/null | head -3\n```\n\n### 2.3 检查配置文件是否存在\n```bash\nls -la /www/jedi_web/config.source.json 2>/dev/null && echo \"CONFIG_OK\" || echo \"CONFIG_MISSING\"\nls -la /www/jedi_web/清华相关微信公众号表.xlsx 2>/dev/null && echo \"ACCOUNTS_OK\" || echo \"ACCOUNTS_MISSING\"\n```\n\n### 2.4 检查最近一次成功采集的时间\n```python3\nimport sqlite3\nconn = sqlite3.connect('/www/jedi_web/wechat_765.sqlite')\nc = conn.cursor()\nc.execute(\"SELECT collected_at, COUNT(*) FROM wechat_articles WHERE content_status='success' GROUP BY substr(collected_at,1,10) ORDER BY substr(collected_at,1,10) DESC LIMIT 5\")\nfor row in c.fetchall():\n    print(f'  {row[0][:10]}: {row[1]} 篇')\nconn.close()\n```\n\n## 3. 异常原因分析\n\n根据以上排查结果，推断异常原因。常见原因及对应处理方案：\n\n| 症状 | 可能原因 | 处理方案 |\n|------|---------|---------|\n| collected=0 且 cron log 无 \"started\" | cron 未触发 | 检查系统 crontab: `crontab -l \\| grep wechat-collector` |\n| collected=0 但 cron log 有报错 | 采集脚本执行失败 | 查看完整错误日志，修复具体问题 |\n| missing 占比高 (>30%) | 微信公众号API限流或网络波动 | 等待下次采集自动补采，或手动执行 `LEAD_REPORT_TRIGGER=0 bash /www/jedi_web/run-wechat-collector-daily.sh` |\n| collected>0 但 success=0 | 文章内容解析全部失败 | 检查公众号表配置是否正确更新 |\n| CONFIG_MISSING 或 ACCOUNTS_MISSING | 配置文件被移动或删除 | 恢复对应文件 |\n| DB 长时间未更新 | 数据库写入权限问题 | 检查磁盘空间 `df -h` 和文件权限 |\n\n## 4. 推送通知\n\n用 notification_send 发送，targetKey=\"何杨文\"。\n\n如果状态正常（✅）：\n```\n📡 科创巡检 · 采集\n━━━━━━━━━━━━━━━\n📅 {today}\n\n✅ 微信文章采集：正常\n  · 02:00批次：{collected} 篇\n  · 成功：{success} | 缺失：{missing} | 空：{empty}\n  · 数据库更新：{db_mtime}\n```\n\n如果异常或警告，必须包含分析结论和处理方案：\n```\n📡 科创巡检 · 采集\n━━━━━━━━━━━━━━━\n📅 {today}\n\n{icon} 微信文章采集：{status}\n  · 02:00批次：{collected} 篇\n  · 成功：{success} | 缺失：{missing} | 空：{empty}\n  · 数据库更新：{db_mtime}\n\n🔍 异常分析：\n  · 原因：[从排查步骤推断的具体原因]\n\n🛠 处理方案：\n  · [从分析表中选择的对应处理方案]\n```"
    },
    {
      "id": "26cd7e13",
      "name": "科创巡检 · 日报",
      "cron": "30 5 * * *",
      "recurring": true,
      "description": "每天05:30检查前一日日报是否生成",
      "template": "📡 科创巡检 · 日报\n━━━━━━━━━━━━━━━\n📅 {yesterday}\n\n{icon} 日报生成：{status}\n  · Skill: daily-investment-leads-report\n  · 状态: {status} | {size} | {time}\n\n{fix}",
      "prompt": "🔍 科创巡检 — 日报巡检 (05:30)\n\n请严格按照下面步骤执行，发现异常时必须深入分析原因并给出处理方案。\n\n## 1. 日报文件检查\n\n```bash\nYESTERDAY=$(date -d \"yesterday\" \"+%Y年%-m月%-d日\" 2>/dev/null || date -d \"yesterday\" \"+%Y年%m月%d日\")\nFILE=$(ls /home/jedi/jedi-web-agent-output/reports/daily/日报\\(${YESTERDAY}\\).md 2>/dev/null)\n\nif [ -n \"$FILE\" ]; then\n  SIZE=$(stat -c%s \"$FILE\" 2>/dev/null)\n  SIZE_KB=$((SIZE / 1024))\n  MTIME=$(stat -c%y \"$FILE\" 2>/dev/null | cut -d. -f1 | awk '{print $2}' | cut -d: -f1-2)\n  FNAME=$(basename \"$FILE\")\n  if [ \"$SIZE_KB\" -gt 5 ]; then\n    echo \"ICON=✅|STATUS=正常|FILE=$FNAME|SIZE=${SIZE_KB}KB|TIME=$MTIME\"\n  else\n    echo \"ICON=⚠️|STATUS=警告|FILE=$FNAME|SIZE=${SIZE_KB}KB|TIME=$MTIME\"\n  fi\nelse\n  echo \"ICON=🔴|STATUS=异常|FILE=无|SIZE=0KB|TIME=-\"\nfi\necho \"YESTERDAY_LABEL=$YESTERDAY\"\n```\n\n## 2. 如果 STATUS=异常 或 STATUS=警告，必须执行以下排查：\n\n### 2.1 检查采集是否正常（日报依赖采集数据）\n```python3\nimport sqlite3\nfrom datetime import datetime, timezone, timedelta\n\nconn = sqlite3.connect('/www/jedi_web/wechat_765.sqlite')\nc = conn.cursor()\nnow = datetime.now(timezone.utc) + timedelta(hours=8)\nyesterday = now - timedelta(days=1)\nyesterday_str = yesterday.strftime('%Y-%m-%d')\n\nc.execute(f\"SELECT COUNT(*) FROM wechat_articles WHERE collected_at >= '{yesterday_str}T00:00:00' AND collected_at < '{yesterday_str}T23:59:59'\")\nyt_count = c.fetchone()[0]\nc.execute(f\"SELECT COUNT(*) FROM wechat_articles WHERE collected_at >= '{yesterday_str}T00:00:00' AND collected_at < '{yesterday_str}T23:59:59' AND project IS NOT NULL AND project != ''\")\nlead_count = c.fetchone()[0]\nprint(f'YT_COUNT={yt_count}|LEAD_COUNT={lead_count}')\nconn.close()\n```\n\n### 2.2 检查日报 trigger 执行日志\n```bash\ntail -100 /www/jedi_web/logs/wechat-collector-cron.log 2>/dev/null | grep -E \"daily.report|trigger:daily|lead.*report|Lead report|success|fail|error|Error|session\" | tail -20\n```\n\n### 2.3 检查最近的日报文件列表\n```bash\nls -lth /home/jedi/jedi-web-agent-output/reports/daily/ 2>/dev/null | head -10\n```\n\n### 2.4 检查 Jedi Agent API 是否可达\n```bash\ncurl -s -o /dev/null -w \"HTTP_%{http_code}\" --max-time 5 http://127.0.0.1:3456/api/health 2>/dev/null || echo \"API_UNREACHABLE\"\n```\n\n## 3. 异常原因分析\n\n根据排查结果推断原因：\n\n| 症状 | 可能原因 | 处理方案 |\n|------|---------|---------|\n| 日报缺失 + 昨日采集为0 | 上游采集失败，日报无数据源 | 先修复采集问题，然后手动触发日报 |\n| 日报缺失 + 采集正常但API不可达 | Jedi Agent 服务未启动 | 重启 Jedi Agent 服务，然后手动触发日报 |\n| 日报缺失 + 采集正常API正常 | trigger 执行出错或模型调用超时 | 检查 cron log 中的具体错误，手动执行 `REPORT_MODEL=DeepSeek-V4-Pro REPORT_WAIT=1 REPORT_TIMEOUT_MS=7200000 REPORT_RETRIES=2 REPORT_RETRY_DELAY_MS=60000 REPORT_AUTH_PHONE=15527109305 REPORT_AUTH_PASSWORD=123456 npm run trigger:daily-report` |\n| 文件偏小(<5KB) | 模型生成不完整或内容截断 | 检查报告文件内容，可能需重新生成 |\n| 多个日报连续缺失 | cron 调度或环境配置问题 | 检查 crontab: `crontab -l \\| grep daily-report` |\n\n## 4. 推送通知\n\n用 notification_send 发送，targetKey=\"何杨文\"。\n\n如果状态正常（✅）：\n```\n📡 科创巡检 · 日报\n━━━━━━━━━━━━━━━\n📅 {yesterday}\n\n✅ 日报生成：正常\n  · Skill: daily-investment-leads-report\n  · 状态: {status} | {size} | {time}\n```\n\n如果异常或警告，必须包含分析结论和处理方案：\n```\n📡 科创巡检 · 日报\n━━━━━━━━━━━━━━━\n📅 {yesterday}\n\n{icon} 日报生成：{status}\n  · Skill: daily-investment-leads-report\n  · 状态: {status} | {size} | {time}\n\n🔍 异常分析：\n  · 昨日采集：[{yt_count}篇，其中线索{lead_count}篇]\n  · 原因：[从排查步骤推断的具体原因]\n\n🛠 处理方案：\n  · [从分析表中选择的对应处理方案]\n```"
    },
    {
      "id": "6099601d",
      "name": "科创巡检 · 综合",
      "cron": "3 7 * * *",
      "recurring": true,
      "db": "/www/jedi_web/wechat_765.sqlite",
      "description": "每天07:03综合巡检：采集检查+日报+周报+月报",
      "template": "📡 科创巡检\n━━━━━━━━━━━━━━━\n📅 {yesterday_date}\n\n{collect_icon} 微信文章采集：{collect_status}\n  · 昨日02:00批次：{c22} 篇\n  · 昨日全天采集：{yt} 篇\n  · 含项目线索：{leads} 篇\n  · 数据库总量：{total} 篇\n\n{daily_icon} 日报生成：{daily_status}\n  · Skill: daily-investment-leads-report\n  · 状态: {daily_status} | {daily_size} | {daily_time}\n\n{weekly_icon} 周报生成：{weekly_status}\n  · Skill: daily-investment-leads-report\n  · 最新: {weekly_file} | {weekly_size}\n  · 状态: {weekly_status}\n\n{monthly_icon} 月报生成：{monthly_status}\n  · Skill: daily-investment-leads-report\n  · 最新: {monthly_file} | {monthly_size}\n  · 状态: {monthly_status}",
      "prompt": "🔍 科创巡检 — 综合巡检 (07:03)\n\n请按下面步骤执行。每项检查发现异常时，立即执行对应的排查命令；最后汇总所有异常、原因分析和处理方案一并推送。\n\n## 1. 采集检查 (/www/jedi_web/wechat_765.sqlite)\n\n```python3\nimport sqlite3, os\nfrom datetime import datetime, timezone, timedelta\n\nconn = sqlite3.connect('/www/jedi_web/wechat_765.sqlite')\nc = conn.cursor()\n\nnow = datetime.now(timezone.utc) + timedelta(hours=8)\nyesterday = now - timedelta(days=1)\nyesterday_str = yesterday.strftime('%Y-%m-%d')\nbatch_utc_date = (now - timedelta(days=2)).strftime('%Y-%m-%d')\n\nutc_start = f'{batch_utc_date}T18:00:00'\nutc_end = f'{batch_utc_date}T18:45:00'\nc.execute(f\"SELECT COUNT(*) FROM wechat_articles WHERE collected_at >= '{utc_start}' AND collected_at < '{utc_end}'\")\nc22 = c.fetchone()[0]\nc.execute(f\"SELECT COUNT(*) FROM wechat_articles WHERE collected_at >= '{yesterday_str}T00:00:00' AND collected_at < '{yesterday_str}T23:59:59'\")\nyt = c.fetchone()[0]\nc.execute(f\"SELECT COUNT(*) FROM wechat_articles WHERE collected_at >= '{yesterday_str}T00:00:00' AND collected_at < '{yesterday_str}T23:59:59' AND project IS NOT NULL AND project != ''\")\nleads = c.fetchone()[0]\nc.execute('SELECT COUNT(*) FROM wechat_articles')\ntotal = c.fetchone()[0]\nc.execute('SELECT MAX(collected_at) FROM wechat_articles')\nlast_coll = c.fetchone()[0] or 'N/A'\n\ncoll_icon = '✅' if c22 > 0 else '🔴'\ncoll_status = '正常' if c22 > 0 else '异常'\n\nprint(f'DATE={yesterday_str}|C22={c22}|YT={yt}|LEADS={leads}|TOTAL={total}|COLL_ICON={coll_icon}|COLL_STATUS={coll_status}|LAST_COLL={last_coll}')\nconn.close()\n```\n\n**如果 COLL_STATUS=异常 (c22=0)，立即排查：**\n```bash\necho \"=== cron 采集日志 ===\"\ntail -80 /www/jedi_web/logs/wechat-collector-cron.log 2>/dev/null | grep -iE \"error|fail|started|finished|command not found\" | tail -15\necho \"=== 最近备份 ===\"\nls -lt /www/jedi_web/backups/wechat_765.backup-*.sqlite 2>/dev/null | head -3\necho \"=== 磁盘空间 ===\"\ndf -h /www/jedi_web/ | tail -1\n```\n\n## 2. 日报检查\n\n```bash\nYESTERDAY=$(date -d \"yesterday\" \"+%Y年%-m月%-d日\" 2>/dev/null || date -d \"yesterday\" \"+%Y年%m月%d日\")\nFILE=$(ls /home/jedi/jedi-web-agent-output/reports/daily/日报\\(${YESTERDAY}\\).md 2>/dev/null)\nif [ -n \"$FILE\" ]; then\n  SIZE=$(stat -c%s \"$FILE\" 2>/dev/null)\n  MTIME=$(stat -c%y \"$FILE\" | cut -d. -f1 | awk '{print $2}' | cut -d: -f1-2)\n  echo \"DAILY_ICON=✅|DAILY_STATUS=生成成功|DAILY_FILE=$(basename $FILE)|DAILY_SIZE=$((SIZE/1024))KB|DAILY_TIME=$MTIME\"\nelse\n  echo \"DAILY_ICON=🔴|DAILY_STATUS=未生成|DAILY_FILE=无|DAILY_SIZE=-|DAILY_TIME=-\"\nfi\n```\n\n**如果 DAILY_STATUS=未生成，立即排查：**\n```bash\necho \"=== 日报 trigger 日志 ===\"\ntail -100 /www/jedi_web/logs/wechat-collector-cron.log 2>/dev/null | grep -iE \"daily.report|trigger:daily|success|fail|error|session\" | tail -15\necho \"=== 最近日报列表 ===\"\nls -lth /home/jedi/jedi-web-agent-output/reports/daily/ 2>/dev/null | head -5\necho \"=== API 可达性 ===\"\ncurl -s -o /dev/null -w \"HTTP_%{http_code}\" --max-time 5 http://127.0.0.1:3456/api/health 2>/dev/null || echo \"API_UNREACHABLE\"\n```\n\n## 3. 周报检查\n\n```bash\nLATEST=$(ls -t /home/jedi/jedi-web-agent-output/reports/weekly/周报*.md 2>/dev/null | head -1)\nif [ -n \"$LATEST\" ]; then\n  SIZE=$(stat -c%s \"$LATEST\")\n  MTIME=$(stat -c%y \"$LATEST\" | cut -d. -f1 | awk '{print $2}' | cut -d: -f1-2)\n  FNAME=$(basename \"$LATEST\" .md)\n  echo \"WEEKLY_ICON=ℹ️|WEEKLY_STATUS=已有|WEEKLY_FILE=$FNAME|WEEKLY_SIZE=$((SIZE/1024))KB|WEEKLY_TIME=$MTIME\"\nelse\n  echo \"WEEKLY_ICON=🔴|WEEKLY_STATUS=缺失|WEEKLY_FILE=无|WEEKLY_SIZE=-|WEEKLY_TIME=-\"\nfi\n```\n\n## 4. 月报检查\n\n```bash\nLATEST=$(ls -t /home/jedi/jedi-web-agent-output/reports/monthly/月报*.md 2>/dev/null | head -1)\nif [ -n \"$LATEST\" ]; then\n  SIZE=$(stat -c%s \"$LATEST\")\n  MTIME=$(stat -c%y \"$LATEST\" | cut -d. -f1 | awk '{print $2}' | cut -d: -f1-2)\n  FNAME=$(basename \"$LATEST\" .md)\n  echo \"MONTHLY_ICON=ℹ️|MONTHLY_STATUS=已有|MONTHLY_FILE=$FNAME|MONTHLY_SIZE=$((SIZE/1024))KB|MONTHLY_TIME=$MTIME\"\nelse\n  echo \"MONTHLY_ICON=🔴|MONTHLY_STATUS=缺失|MONTHLY_FILE=无|MONTHLY_SIZE=-|MONTHLY_TIME=-\"\nfi\n```\n\n## 5. 原因分析\n\n汇总以上所有异常项，按下面诊断表分析原因：\n\n**采集异常：**\n| 症状 | 可能原因 | 处理方案 |\n|------|---------|---------|\n| C22=0 + cron log 无 \"started\" | cron 未执行 | `crontab -l \\| grep wechat-collector` 检查定时任务 |\n| C22=0 + cron log 有 error | 采集脚本报错 | 查看完整日志修复 |\n| C22=0 + 磁盘满 | 磁盘空间不足 | 清理 /www/jedi_web/backups/ 旧备份 |\n| YT 偏少 + missing 多 | API 限流 | 等待自动恢复，下次采集补采 |\n\n**日报异常：**\n| 症状 | 可能原因 | 处理方案 |\n|------|---------|---------|\n| 日报缺失 + 采集为0 | 上游无数据 | 先修复采集 |\n| 日报缺失 + 采集正常 + API不通 | 服务未启动 | 重启 Jedi Agent |\n| 日报缺失 + API正常 | trigger 失败 | 手动执行 trigger:daily-report |\n\n**周报/月报注意：** 周报在每周五生成，月报在每月1日生成。如果未到生成日显示缺失为正常（显示ℹ️）。如果到了生成日仍缺失，检查对应 cron: `crontab -l \\| grep weekly-report` / `crontab -l \\| grep monthly-report`。\n\n## 6. 汇总推送\n\n用 notification_send 发送，targetKey=\"何杨文\"。\n\n### 全部正常时：\n```\n📡 科创巡检\n━━━━━━━━━━━━━━━\n📅 {yesterday_date}\n\n✅ 微信文章采集：正常\n  · 昨日02:00批次：{c22} 篇\n  · 昨日全天采集：{yt} 篇\n  · 含项目线索：{leads} 篇\n  · 数据库总量：{total} 篇\n\n✅ 日报生成：正常\n  · Skill: daily-investment-leads-report\n  · 状态: {daily_status} | {daily_size} | {daily_time}\n\nℹ️ 周报生成：{weekly_status}\n  · 最新: {weekly_file} | {weekly_size}\n\nℹ️ 月报生成：{monthly_status}\n  · 最新: {monthly_file} | {monthly_size}\n```\n\n### 有异常时，在推送末尾追加分析段：\n```\n📡 科创巡检\n━━━━━━━━━━━━━━━\n📅 {yesterday_date}\n\n{coll_icon} 微信文章采集：{coll_status}\n  · 昨日02:00批次：{c22} 篇\n  · 昨日全天采集：{yt} 篇\n  · 含项目线索：{leads} 篇\n  · 数据库总量：{total} 篇\n\n{daily_icon} 日报生成：{daily_status}\n  · Skill: daily-investment-leads-report\n  · 状态: {daily_status} | {daily_size} | {daily_time}\n\n{weekly_icon} 周报生成：{weekly_status}\n  · 最新: {weekly_file} | {weekly_size}\n  · 状态: {weekly_status}\n\n{monthly_icon} 月报生成：{monthly_status}\n  · 最新: {monthly_file} | {monthly_size}\n  · 状态: {monthly_status}\n\n━━━━━━━━━━━━━━━\n🔍 异常分析：\n（逐项列出：异常项 → 排查发现 → 推断原因）\n\n🛠 处理方案：\n（逐项给出可执行的具体操作命令或步骤）\n```"
    },
    {
      "id": "22573411",
      "name": "续期提醒（监控+舆情）",
      "cron": "7 9 22 6 *",
      "recurring": false,
      "description": "一次性提醒：服务器监控和美的舆情统计即将到期",
      "template": "⏰ 定时任务续期提醒\n\n以下任务将于明天到期，请确认是否需要续期：\n  · 服务器资源监控 (409861ad)\n  · 美的舆情统计 (1c975f7b)",
      "prompt": "提醒：两个定时任务（服务器监控 409861ad 和美的舆情统计 1c975f7b）将于明天到期。请通知用户（targetKey=\"何杨文\"）是否需要续期，使用 notification_send 发送提醒消息。"
    },
    {
      "id": "07a33078",
      "name": "续期提醒（3个巡检）",
      "cron": "7 9 22 6 *",
      "recurring": false,
      "description": "一次性提醒：三个科创巡检任务即将到期",
      "template": "⏰ 定时任务续期提醒\n\n以下科创巡检任务将于明天到期，请确认是否需要续期：\n  · 采集巡检 (bb0773c9)\n  · 日报巡检 (26cd7e13)\n  · 综合巡检 (6099601d)",
      "prompt": "提醒：三个科创巡检定时任务（采集巡检 bb0773c9、日报巡检 26cd7e13、综合巡检 6099601d）即将到期。请通知用户（targetKey=\"何杨文\"）是否需要续期，使用 notification_send 发送提醒消息。"
    }
  ],
  "import_instructions": "服务重启后，对每个 job 调用 CronCreate，参数为 cron、prompt、recurring。"
}
